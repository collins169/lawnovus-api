export const HUNDRED_MS = 100;

export const ONE_SECOND = 1000;

export const ONE = 1;
export const TWO = 2;
export const THREE = 3;
export const FOUR = TWO * TWO;
export const FIVE = TWO + THREE;
export const TEN = TWO * FIVE;

export const THREE_SECONDS = ONE_SECOND * THREE;

export const FIVE_SECONDS = ONE_SECOND * FIVE;

export const SIX_SECONDS = ONE_SECOND * (FIVE + ONE);

export const EIGHT_SECONDS = ONE_SECOND * (FIVE + THREE);

export const TEN_SECONDS = ONE_SECOND * (FIVE * TWO);

export const FIFTEEN_SECONDS = ONE_SECOND * (FIVE * THREE);

export const THIRTY_SECONDS = ONE_SECOND * (TEN * THREE);

export const FORTY_FIVE_SECONDS = ONE_SECOND * (THREE * THREE * FIVE);

export const ONE_MINUTE = ONE_SECOND * (TEN * THREE * TWO);

export const TWO_MINUTES = ONE_MINUTE * TWO;

export const FIVE_MINUTES = ONE_MINUTE * FIVE;

export const TEN_MINUTES = ONE_MINUTE * TEN;

export const TWENTY_MINUTES = TWO_MINUTES * TEN;

export const THIRTY_MINUTES = ONE_MINUTE * (THREE * TEN);

export const THREE_HUNDRED_MS = HUNDRED_MS * THREE;
